{% block header %}
<header class="sticky-top">
	<nav class="navbar navbar-expand-md navbar-dark bg-dark py-0">
		<a class="navbar-brand p-0" href="/">
			<img src="/images/admin/logo.png" width="50" alt="Logo du site">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarHeader">
			<ul class="navbar-nav me-auto">
				{{ NavLink('home', 'Lien vers une page') | raw }}
				{{ NavLink('home', 'Accueil') | raw }}
			</ul>
			
			<ul class="navbar-nav text-end">
				{{ NavLink('login', 'Login', 0) | raw }}
				{{ NavLink('logout', 'Logout', 1) | raw }}
				{{ NavLink('page', 'Page') | raw }}
			</ul>
		</div>

	</nav>
</header>
<div class="container">{{ modal|raw }}</div>
{% endblock header %}